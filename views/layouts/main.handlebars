<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vauclain Tech News</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" rel="stylesheet" />
</head>

<body class="bg-light">

  <div class="container">
    <header
      class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
        Vauclain Tech News
      </a>

      <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
        {{#if loggedIn}}<li><a href="/dashboard" class="nav-link px-2 link-dark">Dashboard</a></li>{{/if}}
      </ul>

      <div class="col-md-3 text-end">
        {{#if loggedIn}}
        <button id="logout" type="button" class="btn btn-outline-primary me-2">Logout</button>
        {{else}}
        <button type="button" class="btn btn-outline-primary me-2"><a class="text-decoration-none"
            href="/login">Login</a></button>
        {{!-- <button type="button" class="btn btn-primary"><a href="/login"></a>Sign-up</a></button> --}}
        {{/if}}
      </div>
    </header>
  </div>


  <div class="container">
    <div class="d-flex align-items-center p-3 my-3 bg-primary text-white rounded shadow-sm">
      <i class="fs-1 fas fa-database me-3"></i>
      <div class="lh-1">
        <h1 class="h6 mb-0 text-white lh-1">Your Daily Source</h1>
        <small>For all the latest and greatest in tech</small>
      </div>
    </div>

    {{{ body }}}

    <footer class="px-2 mb-2 bg-dark text-light p-3">
      Thanks for visiting Vauclain Tech News!
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

</body>

</html>